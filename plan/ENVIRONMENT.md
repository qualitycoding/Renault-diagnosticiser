# Environment (pinned, verified in sandbox 2026-09-24)

| Component | Version |
|---|---|
| Python | 3.12.3 (any 3.12.x accepted; `requires-python >=3.12,<3.13`) |
| obd (python-OBD) | 0.7.3 |
| pyserial | 3.5 |
| pint (via obd) | 0.24.4 |
| pytest | 9.1.1 |
| ELM327-emulator (dev only, CC-BY-NC-SA-4.0) | 4.0.0 |
| pip-audit | 2.10.1 |
| setuptools (build) | 84.0.0 |
| pip-tools (re-locking only) | 7.5.1 |

Lockfiles: `requirements.txt`, `requirements-dev.txt` (hash-pinned, generated by `pip-compile --generate-hashes --strip-extras --allow-unsafe` from the `.in` files).

## Setup (executed successfully in the sandbox)
```bash
python3.12 -m venv .venv
. .venv/bin/activate            # Windows: .venv\Scripts\activate
pip install --require-hashes -r requirements.txt -r requirements-dev.txt
pip install --no-deps --no-build-isolation -e .
pip-audit -r requirements.txt && pip-audit -r requirements-dev.txt   # expect "No known vulnerabilities found" twice
sha256sum -c tests/FROZEN_MANIFEST.sha256                          # expect every line OK
pytest -q                                                           # at plan time: all tests fail/skip RED (stubs)
```
Serial access on Linux: `sudo usermod -aG dialout $USER` then re-login (real hardware only; tests use a pty).
